<h1>You sent the following data in a {{{reqType}}} request:</h1>
<ul>
  {{#each dataList1}}
    <li>{{this.name}}: {{this.value}}
  {{/each}}
</ul>

<ul>
  {{#each dataList2}}
    <li>{{this.name}}: {{this.value}}
  {{/each}}
</ul>

